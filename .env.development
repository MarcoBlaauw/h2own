# Local development defaults (safe to commit).
# Override per-machine secrets in .env.development.local or .env.local.

NODE_ENV=development
PORT=3001
LOG_LEVEL=info

# Keep browser calls same-origin; Vite/SvelteKit proxy /api to INTERNAL_API_URL.
VITE_API_URL=/api
PUBLIC_API_BASE=/api
INTERNAL_API_URL=http://localhost:3001
APP_BASE_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Local infra defaults
DATABASE_URL=postgres://h2own:changeme@localhost:5432/h2own
REDIS_URL=redis://localhost:6379
TRUST_PROXY=false
